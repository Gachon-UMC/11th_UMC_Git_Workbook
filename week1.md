## PK, FK란?

**Primary Key**
테이블 내에서 각 행을 식별할 수 있는 값. 중복을 허용하지 않으며 NOT NULL. `member_id` 같은 값.

**Foreign Key**
다른 테이블의 PK를 참조하는 값.

## ERD란?

Entity-Relationship-Diagram. DB 내에 어떤 Entity들이 들어있고 그 Entity들의 관계를 그림으로 표현한 것.

## 연관관계란? 그리고 연관관계를 설정하는 방법은?

연관관계는 테이블(자바에서는 엔티티) 사이의 관계를 설정해주는 것. 1:N, N:M 관계가 있다. N:M 연관관계에서는 중간 테이블을 하나 만들어서 양쪽 엔티티의 PK를 가져와 FK로 사용한다.

자바에서는 `@ManyToOne`, `@OneToMany`, `@ManyToMany` 어노테이션으로 표현하고 단방향과 양방향을 선택 가능하다.

## 정규화란?

데이터 중복을 최소화하고 정합성(데이터가 모순되지 않고 일관되게)을 지키기 위해서 테이블을 쪼개는 과정.

## 반정규화란?

정규화의 반대로, 데이터의 중복을 어느 정도 허용한다. 정규화 정도가 높을수록 테이블이 많아지고, 데이터를 조회할 때 여러 테이블을 JOIN 해야 하기 때문에 속도가 느려질 수 있다.

## DB에서의 상속 관계 표현은 어떻게 하는가?

**단일 테이블 전략**
부모 자식 클래스를 전부 한 테이블에 넣어놓고 `dtype`으로 어떤 자식인지 표현. 단일 테이블이기 때문에 조인이 없어서 빠름. 자식 컬럼을 다 가지고 있어야 하기 때문에 사용하지 않는 컬럼은 null이 됨. 자식 컬럼은 모두 nullable해야 한다.

**조인 전략**
부모 테이블, 자식 테이블을 각각 만들고 자식 테이블이 부모의 PK를 FK이자 자신의 PK로 사용한다. 공통 필드는 부모 테이블에 넣고 자식만의 필드는 자식 테이블에 넣는다. 조회 시 조인 필요.

**구현 클래스마다 테이블 전략**
그냥 자식 클래스마다 테이블을 따로 만들어서 사용. 여러 테이블을 조회해야 할 때 느리다. 안 씀.

## 인덱스란?

특정 컬럼으로 검색할 때 빠르게 찾을 수 있도록 한 자료구조. PK는 자동으로 인덱스 처리된다. 자주 검색 조건으로 쓰이는 컬럼에 인덱스를 걸면 조회 속도가 빨라진다. 인덱스를 사용하면 조회 속도는 빨라지지만, 인덱스가 너무 많아지면 데이터를 추가 및 수정할 때마다 인덱스를 갱신해야 하므로 쓰기 속도는 느려진다.