\- TypeScript와 정적 타입 검사

&#x20;   - JavaScript와 비교했을 때 TypeScript는 오류를 언제, 어떤 방식으로 확인하나요?

&#x20;    - ts는 컴파일 실행 전 오류를 미리 확인하고, js는 런타임 실행 중 오류를 확인할 수 있다.

&#x20;   - 컴파일 타임 오류와 런타임 오류는 어떤 차이가 있나요?

&#x20;    － 컴파일 타임 오류는 코드를 실제로 실행하기 전 컴파일 과정에서 발견되는 오류이고,

&#x20;   런타임 오류는 코드가 실행되는 과정에서 발생하는 오류이다.

&#x20;   - TypeScript의 타입 정보가 실행되는 JavaScript에 남지 않는 이유는 무엇일까요?

&#x20;    - 컴파일 타임에서 ts 코드의 타입 검증을 마쳤기 때문에 js 코드에서는 더 이상 필요 없는

&#x20;   타입 데이터가 지워지는 것이다. 또 성능 최적화와 js의 순수성 유지를 위한 목적도 있다.

\- 타입 추론과 타입 모델링

&#x20;   - TypeScript가 타입을 추론하도록 두는 경우와 타입을 직접 작성하는 경우는 각각 언제 알맞을까요?

&#x20;    - 타입 추론 : 변수를 선언하면서 곧바로 원시값을 할당할 때

&#x20;    - 타입 직접 작성 : 함수의 파라미터, 함수가 올바르게 작동하기 위해서 그에 맞은 input 값이 필요

&#x20;   - 객체와 함수의 형태를 타입으로 표현하면 어떤 실수를 미리 찾을 수 있을까요?

&#x20;    - 객체에 필요한 데이터가 빠진 상태로 생성되는 것을 방지한다.

&#x20;    - 함수의 파라미터를 타입으로 표현해두면 데이터 형식에 관한 오류를 사전에 방지한다.

&#x20;   - `type`과 `interface`는 표현 범위와 확장 방식에서 어떤 차이가 있을까요?

&#x20;    - `type` : 객체 뿐만 아니라 원시 타입, 유니온 타입 등 거의 모든 타입을 이름 지어 표현할 수 있고,

&#x20;   \&를 사용해 여러 객체 타입을 하나로 조합할 수 있다.

&#x20;    - `interface` : 객체의 구조를 정의하는데 특화되어 있고,

&#x20;   동일한 이름으로 `interface`를 정의하면 합쳐지는 기능이 있다.

\- 유니언 타입과 타입 좁히기

&#x20;   - 여러 타입 중 하나가 될 수 있는 값을 유니언 타입으로 표현하면 어떤 장점이 있나요?

&#x20;    - 엉뚱한 타입에 값이 들어오는 실수를 사전에 막으면서도, 허용된 타입 내에서는

&#x20;   자유롭게 데이터가 들어올 수 있게 한다.

&#x20;   - 조건문, `typeof`와 판별 프로퍼티는 타입을 어떻게 좁히나요?

&#x20;    - `typeof`  : `typeof value === “string”`  해당 코드와 같이 조건문으로 사용 가능하다.

&#x20;    - 판별 프로퍼티 :  객체마다 고유한 속성을 두어 구분한다.

&#x20;   

&#x20;   ```jsx

&#x20;   type Admin = {

&#x20;     tag: "ADMIN"; // 👈 판별자(Discriminant) 역할의 고정된 값

&#x20;     adminCode: string;

&#x20;   };

&#x20;   

&#x20;   type User = {

&#x20;     tag: "USER"; // 👈 판별자(Discriminant) 역할의 고정된 값

&#x20;     points: number;

&#x20;   };

&#x20;   

&#x20;   // 두 타입을 유니언으로 묶음

&#x20;   type Member = Admin | User;

&#x20;   ```

&#x20;   

&#x20;   - `null`과 `undefined`가 포함된 값을 안전하게 다룰 때 옵셔널 체이닝과 널 병합 연산자는 어떤 역할을 하나요?

&#x20;    - 옵셔널 체이닝 : `const githubId = foundMember?.githubId;` 

&#x20;   `foundMember`의 값이 `null` 또는 `undefined`일 때 `githubId`에 값을 조회해도 오류 내지 않고

&#x20;   `undefined`를 반환한다.

&#x20;   - 널 병합 연산자 : 

&#x20;   `console.log(date ?? 1)`  ⇒ data의 값이 `null` 또는 `undefined`이면 오른쪽 값을 반환한다.

&#x20;   `console.log(date || 1)`  ⇒ data의 값이 falsy이면 오른쪽 값을 반환한다.

\- 타입 안전성과 제네릭

&#x20;   - `any`와 `unknown`은 타입 검사를 허용하는 방식이 어떻게 다른가요?

&#x20;    - `any` : 타입 검사를 끄는 기능이라 봐도 무방하다. 타입 오류를 놓치기 쉽다.

&#x20;    - `unknown`  : 어떤 타입의 값이든 받을 수 있지만, 해당 값을 사용하기 전에 타입 검증을해야 한다.

&#x20;   - 여러 타입을 받을 때 `unknown`과 제네릭은 각각 어떤 상황에 알맞을까요?

&#x20;    - `unknown` : 입력된 데이터의 정체를 전혀 예측할 수 없을 때

&#x20;    - 제네릭 : 함수 인자의 타입과 리턴 타입을 연동시키고 싶을 때

&#x20;   - 제네릭은 입력 타입과 출력 타입의 관계를 어떻게 유지하나요?

&#x20;    - 함수에 특정 타입을 선언하면 리턴 타입에도 똑같이 타입이 적용된다.

